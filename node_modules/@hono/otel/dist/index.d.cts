import { Context, MiddlewareHandler } from 'hono';
import { TracerProvider, MeterProvider, Tracer, TimeInput } from '@opentelemetry/api';

type HttpInstrumentationConfig = {
    tracerProvider?: TracerProvider;
    meterProvider?: MeterProvider;
    tracer?: Tracer;
    captureRequestHeaders?: string[];
    captureResponseHeaders?: string[];
    getTime?(): TimeInput;
    disableTracing?: boolean;
    spanNameFactory?: (c: Context) => string;
    serviceName?: string;
    serviceVersion?: string;
};

declare const httpInstrumentationMiddleware: (userConfig?: HttpInstrumentationConfig) => MiddlewareHandler;

export { httpInstrumentationMiddleware };
